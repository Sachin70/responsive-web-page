import FeedbackCarousel from '@/components/FeedbackCarousel';

const AboutUs = () => {
    return (
        <div className="text-center font-bold py-10">
            Work In Progress
            <FeedbackCarousel heading="sachin" leftScrollValue={400} rightScrollValue={400} />
        </div>
    );
};

export default AboutUs;
